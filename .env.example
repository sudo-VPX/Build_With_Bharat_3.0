# CloudGuard never stores AWS keys in this file. Authenticate with the AWS CLI
# or an AWS-supported credential provider, then select the profile below.
CLOUDGUARD_AWS_PROFILE=CloudGuard
CLOUDGUARD_AWS_REGION=eu-north-1
CLOUDGUARD_LOOKBACK_DAYS=90
CLOUDGUARD_DEFAULT_MODE=demo
